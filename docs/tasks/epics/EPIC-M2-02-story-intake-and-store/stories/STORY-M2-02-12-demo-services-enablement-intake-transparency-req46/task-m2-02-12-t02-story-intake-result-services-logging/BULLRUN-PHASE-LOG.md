# BULLRUN — TASK-M2-02-12-T02

- [x] `StoryIntakeResult` dataclass; `create_story()` returns result
- [x] `intake.geo_not_resolved` INFO when non-empty `location_query` misses
- [x] `intake.geo_skip` DEBUG when no `location_query`
- [x] `intake.gpt_signals_drop` WARNING when store is None; `_persist_*` returns bool

# Acceptance — STORY-M2-10-03

- `build_adapter_bundle(load_config_from_env(...))` возвращает согласованный набор адаптеров.
- `adapter_runtime_flags` отражает `deployment_profile` и три флага из `FeatureFlags`.

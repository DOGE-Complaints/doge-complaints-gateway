# Run Summary — 20260422-2038

## Run metadata

- Timestamp: `2026-04-22 20:38` (local)
- Project: `doge-complaints-gateway`
- Mode: `explicit` (план: runtime verification + удаление legacy `dev_server`)
- Scope: HTTP smoke на живом сервере, Puppeteer mock auth, удаление `src/core/api/dev_server.py`, регрессия тестов

## Process coverage

- Фактическая проверка сервера: выполнена (`curl` к `/health`, `/ready`, `/protected/status`, `/metrics`).
- Puppeteer UI smoke: выполнен (временная установка `puppeteer` в `$TMPDIR`, сценарий CTA → loading → success + identity text).
- Удаление legacy: `dev_server.py` удалён, ссылки в task/docs и `SOURCES.txt` обновлены.
- Регрессия: `python3 -m pytest tests/ -q` после правки `_pilot_env()` в `tests/test_adapters_demo_pilot.py`.

## Executed items

- Удалён `src/core/api/dev_server.py` (файл больше не входит в дерево исходников).
- Обновлены task-артефакты и run-summary `20260422-1730` (follow-up примечание).
- Исправлен [`tests/test_adapters_demo_pilot.py`](../../../tests/test_adapters_demo_pilot.py): pilot env включает `SERVICE_API_TOKEN` и `REQUEST_TIMEOUT_S`.

## Acceptance summary

### HTTP (живой процесс, `PORT=8765`, `SERVICE_API_TOKEN=smoke-test-token`)

| Endpoint | Без Bearer | С Bearer |
|----------|------------|----------|
| `GET /health` | 200, `data.status=ok` | — |
| `GET /ready` | 200, `data.status=ready` | — |
| `GET /protected/status` | 401 `UNAUTHORIZED` | 200 `authenticated` |
| `GET /metrics` | 401 `UNAUTHORIZED` | 200 counters envelope |

### Puppeteer (`/demo/auth-page`)

- CTA видим, клик, loading без `.hidden`, success без `.hidden`, текст содержит `demo-user-42` и `identity_issuer`.
- Итог консоли: `PUPPETEER_SMOKE_OK`.

### Pytest

- `104 passed` (полный `tests/`).

## Commit summary

- Коммиты в этом run: не выполнялись.

## Open follow-ups

1. При желании закрепить Puppeteer smoke как повторяемый скрипт в репозитории (сейчас проверка была через ephemeral `npm install` в `$TMPDIR`).
2. Закоммитить изменения по методике репозитория.

## Task: implement — centralized config injection consistency

### AC/DoD
- [x] `AppConfig` централизованно предоставляется через infrastructure providers.
- [x] `DefaultServiceFactory` и `ApiDependencies` используют единый config объект.

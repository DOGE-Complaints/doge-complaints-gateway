# Acceptance — STORY-M2-13-02

- В `parse_story_intake_request` валидируются `origin/privacy/live_story_context`.
- В `StoryRecord` сохраняются origin/privacy поля без breaking change существующих payload.
